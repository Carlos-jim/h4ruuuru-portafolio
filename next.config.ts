/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["cdn4.iconfinder.com", "img.icons8.com", "icons8.com"],
  },
};

module.exports = nextConfig;
